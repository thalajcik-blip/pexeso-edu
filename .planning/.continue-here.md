---
context: default
phase: pub-quiz-feature (outside GSD phases)
task: testing-setup
total_tasks: ~8
status: in_progress
last_updated: 2026-04-26T13:29:05Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not constraints from failure — advisory items below.

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `profiles.roles` vs `user_roles` table | Teacher gate in pub quiz reads `profiles.roles` (array), NOT `user_roles` table — editing `user_roles` has no effect | advisory | Always check CreateSession.tsx gate logic before DB edits |

<current_state>
Pub Quiz feature (Phase 1) je nakódovaná a deploynutá na develop branch. DB tabuľky sú vytvorené v dev Supabase. Blokuje nás teacher gate — user má v `profiles.roles` stále `['player']` namiesto `['superadmin']`. SQL na update bol poskytnutý ale nespustený.

Supabase MCP server nastavenie bolo začaté (user vybral možnosť B — trvalé nastavenie) ale prerušené kvôli vyčerpaniu kontextu.
</current_state>

<completed_work>

Completed this session:
- DB migrácia: 4 tabuľky vytvorené v dev Supabase SQL Editore (pub_quiz_sessions, rounds, teams, answers)
- `src/types/pubQuiz.ts` — typy pre pub quiz
- `src/services/pubQuizService.ts` — Supabase Realtime service (channel, CRUD)
- `src/store/pubQuizStore.ts` — Zustand store (host + team actions, event handler)
- `src/utils/buildPubQuizQuestions.ts` — question generator (extracted pattern)
- `src/pub-quiz/PubQuizApp.tsx` — router
- `src/pub-quiz/CreateSession.tsx` — session creation s konfiguráciou kôl
- `src/pub-quiz/HostView.tsx` — host panel (lobby, intro, otázky, pauza, results reveal, finál)
- `src/pub-quiz/TeamView.tsx` — team view (join, lobby, otázky, submit, výsledky)
- `src/pub-quiz/DisplayView.tsx` — projektor view (read-only, fullscreen-ready)
- `src/main.tsx` — routing `/pub-quiz/*` → PubQuizApp
- Build prechádza čisto (TypeScript + Vite)
- Commitnuté a pushnuté na develop branch (commits: c130443, f327c5f)
</completed_work>

<remaining_work>

IMMEDIATE (na odblokanie testovania):
- Spustiť SQL v Supabase dev SQL Editore:
  ```sql
  UPDATE profiles SET roles = '{superadmin}'
  WHERE id = (SELECT id FROM auth.users WHERE email = 'thalajcik@gmail.com');
  ```
- Otestovať celý flow: create session → host lobby → team join → otázky → výsledky

FÁZA 2 (po testovaní):
- Supabase MCP server nastavenie (user vybral B — trvalé): potrebuje personal access token z supabase.com/dashboard/account/tokens, potom `claude mcp add` príkaz
- Vercel rewrite pre `/pub-quiz/*` (už funguje cez generický `(.*)` v vercel.json)
- QR kód pre tímy v host lobby (napr. qrcode.js)
- Fix: `hostStartRound` nie je volaný — `hostStartSession` skáče priamo na `hostStartQuestion(0)`, treba prejsť cez round intro
- Timer test — broadcast každú sekundu na mobile
- Finálne výsledky celej session (status `finished`)

FÁZA 3 (neskôr):
- Pro tier gate (doriešiť keď bude subscription_tier v profiles)
- Custom decky v pub quiz (round config rozšíriť o deck picker)
- Multiplayer stress test (8 tímov)
</remaining_work>

<decisions_made>

- Teacher gate zatiaľ = `profile.roles.includes('teacher') || 'superadmin' || 'admin'` — Pro tier neskôr
- Questions pre pub quiz generujeme rovnakou logikou ako Lightning Quiz (reused buildPubQuizQuestions utility)
- Timer beží na host strane a broadcastuje `timer_tick` každú sekundu (nie server-side)
- Answers sa ukladajú do DB (INSERT/upsert) a broadcast je len notifikácia (bez answer content)
- Scoring robí host po každej otázke — číta DB, vypočíta, updateuje total_score na teams
- Projektor view je pure read-only — nepotrebuje auth
- Team join je anonymous (žiadny Supabase auth pre tímy)
- Routing: `isPubQuiz = pathname.startsWith('/pub-quiz')` → `BrowserRouter basename="/pub-quiz"`
</decisions_made>

<blockers>
- Teacher gate blokuje prístup k `/pub-quiz/create` — fix: SQL UPDATE profiles (viď IMMEDIATE vyššie)
- Supabase MCP nie je nastavený — na DB operácie treba SQL Editor alebo credentials
</blockers>

## Required Reading (in order)
1. `src/pub-quiz/` — všetky súbory pub quiz feature
2. `src/store/pubQuizStore.ts` — state management + event handling
3. `src/services/pubQuizService.ts` — Realtime channel + DB ops
4. Brief: `/Users/tomashalajcik/Downloads/pexedu_pub_quiz.md`

## Infrastructure State
- develop branch: commit f327c5f pushnutý, Vercel deploy prebieha/hotový
- Dev Supabase (zmiwnqiocdolvnzabcrm): 4 pub_quiz tabuľky vytvorené, teacher_requests.email stĺpec pridaný
- Dev server bol spustený na localhost:5174 (môže stále bežať)

<context>
Pub Quiz je nová veľká feature mimo GSD phase systému — implementovali sme ju priamo. Architektúra je clean: service layer → store → komponenty. Realtime cez Supabase Broadcast channel `pub-quiz-{sessionCode}`. Tri views (host/team/display) sú kompletné a deploynuté. Hlavný bloker je len permissions v dev DB.
</context>

<next_action>
1. Spustiť SQL UPDATE profiles (viď IMMEDIATE) v dev Supabase SQL Editore
2. Otvoriť dev URL: https://pexeso-edu-git-develop-tomas-halajciks-projects.vercel.app/pub-quiz/create
3. Otestovať celý flow na mobile + desktop
4. Reportovať bugy → opravovať
</next_action>
